@page "/templates/approvals"
@layout DashboardLayout

@* LAYOUT SCAFFOLD (ZD-20 / FR-F4) — static placeholders, no data wiring yet. *@

<div class="w-full grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- queue -->
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 overflow-hidden">
        <div class="px-6 py-5 border-b border-slate-200 flex items-center gap-3 bg-white/90">
            <div class="w-8 h-8 bg-green-600 rounded-xl flex items-center justify-center text-white shadow-sm">
                <i class="fas fa-inbox text-sm"></i>
            </div>
            <h1 class="text-xl font-semibold text-slate-800 tracking-tight">Template approvals</h1>
        </div>
        <div class="p-4 space-y-2">
            @foreach (var name in new[] { "Master Services Agreement", "Employment Contract", "Vendor NDA" })
            {
                <div class="flex items-center justify-between p-3 rounded-xl border border-slate-200 hover:bg-slate-50 cursor-pointer">
                    <div>
                        <p class="font-medium text-slate-800">@name</p>
                        <p class="text-xs text-slate-500">v1 · submitted 2h ago</p>
                    </div>
                    <MudChip T="string" Size="Size.Small" Color="Color.Warning" Variant="Variant.Filled">Pending</MudChip>
                </div>
            }
        </div>
    </div>

    <!-- preview + actions -->
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 p-5 flex flex-col">
        <h2 class="text-sm font-semibold text-slate-700 uppercase tracking-wide mb-3">Review</h2>
        <div class="flex-1 min-h-[420px] rounded-xl border border-dashed border-slate-300 bg-slate-50 flex items-center justify-center text-slate-400">
            <div class="text-center">
                <i class="fas fa-file-pdf text-3xl mb-2"></i>
                <p>Select a template to preview</p>
            </div>
        </div>
        <div class="flex items-center gap-2 pt-4 border-t border-slate-200 mt-4">
            <MudButton Variant="Variant.Filled" Color="Color.Success" StartIcon="@Icons.Material.Filled.CheckCircle">Approve</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Error" StartIcon="@Icons.Material.Filled.Cancel">Reject</MudButton>
        </div>
    </div>
</div>
